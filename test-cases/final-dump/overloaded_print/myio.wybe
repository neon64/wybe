# pragma no_standard_library  # Standard library can't depend on itself!
# use wybe.char
# use wybe.bool
# use wybe.int

use overloaded_print.mylist, overloaded_print.mybool

pub def {impure} myprint(x: char) {
    ?x2 = x
}

pub def {impure} myprint(x:mylist(int)) {
    ?x2 = x
}